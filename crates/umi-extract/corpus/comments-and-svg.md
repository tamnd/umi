# Chart of the approaches

The chart above is drawn in the page rather than loaded as an image, which means every label in it is text that a naive extractor will happily mix into the prose. The drop list exists so that it does not.

The two channels carry different depths and the bar between them moves, which is why the chart is redrawn every year and why the printed version has a date on it.