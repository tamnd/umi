[package]
name = "umi-seed"
description = "Common Crawl, sitemap, feed and corpus seeding"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
quick-xml = { workspace = true }
thiserror = { workspace = true }
umi-types = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

# No criterion, for the same reason umi-file's bench gives: the bar is doc
# 08.1's admission rate in whole URLs a second, not a microsecond difference.
[[bench]]
name = "seeding"
harness = false
