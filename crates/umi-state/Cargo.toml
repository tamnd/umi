[package]
name = "umi-state"
description = "The State trait and the pay level domain shard model"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
umi-types = { workspace = true }
async-trait = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
# Only to drive the async tests. Nothing in this crate needs a runtime, and
# nothing in it spawns, so a backend is free to be built on something else.
tokio = { workspace = true, features = ["rt", "macros"] }
